# Server
PORT=5000
NODE_ENV=development

# MongoDB
MONGO_URI=mongodb://127.0.0.1:27017/trusthub

# JWT (for future auth — not wired yet)
JWT_SECRET=change_me_to_a_long_random_string
JWT_EXPIRES_IN=7d

# Blockchain (for future integration)
# ETHEREUM_RPC_URL=
# CONTRACT_ADDRESS=
# PRIVATE_KEY=
